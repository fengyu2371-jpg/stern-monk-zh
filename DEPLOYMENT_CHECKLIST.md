# Railway 部署前測試清單

## 1. 資料庫安全閘門

- [ ] 暫停正式 Bot，避免備份期間仍有寫入。
- [ ] 對 Railway Volume 的 `monk.db` 建立可下載、可還原的完整備份。
- [ ] 在備份副本執行 `PRAGMA quick_check;`，結果應為 `ok`。
- [ ] 記錄各玩家資料表的筆數，至少包含 `student_profiles`、`student_places`、`oracle_pages`、`town_life_players`、`town_life_inventory`。
- [ ] 確認沒有 `oracle_pages_limited_backup`。若存在，停止部署並人工比對 `oracle_pages` 與 backup；不要自行刪表。
- [ ] 先用正式資料庫的副本啟動修正版。若 log 顯示「schema 不完整」或「停止自動改建」，停止部署，不要在正式資料庫上嘗試補欄。
- [ ] 副本啟動後重新核對上述各表筆數，神諭與玩家資料不可減少。

## 2. 檔案與環境

- [ ] Railway 專案根目錄可直接看到 `main.py`、`town_life.py`、`requirements.txt`、`railway.toml`、`data/`、`assets/`。
- [ ] Linux 大小寫完全相符：`assets/town_life`、`assets/town_life/items` 與程式內檔名一致。
- [ ] `data/dialogue.json` 存在且為 UTF-8 JSON；舊神父 Bot 的教學／FAQ 檔案不應再存在。
- [ ] 執行 `python -m py_compile main.py town_life.py`。
- [ ] 執行 `pip install -r requirements.txt`，確認安裝 `discord.py>=2.5,<3.0` 與 `openai>=1.0,<3.0`。
- [ ] 設定 `MONK_TOKEN`、`MONK_CHANNEL_ID`、`MONK_DB_PATH`；需要 AI 時再設定 `OPENAI_API_KEY` 與相應開關。
- [ ] `MONK_DB_PATH` 指向持久化 Railway Volume，不是 ephemeral 專案目錄。
- [ ] Railway start command 為 `python main.py`。

## 3. 指令與面板

- [ ] `/我的`、`/學生資料`、`/城下町` 各自都能在 3 秒內先顯示 Discord 正在處理，之後正常出現面板。
- [ ] 連續輸入同一指令兩次：新面板保留按鈕；舊面板只移除操作元件，公開 embed 仍保留。
- [ ] 交錯輸入 `/我的`、`/學生資料`、`/城下町`，確認最後建立的訊息是唯一可操作面板。
- [ ] 等待五分鐘且不操作：按鈕／選單消失、附件圖片清除、顯示鎖定畫面。
- [ ] 舊面板計時器到期時，新面板不得被關閉。
- [ ] 在表單開啟後另建新面板，再送出舊表單：應顯示未寫入提示，資料不可變更。
- [ ] 故意觸發測試環境 callback 例外：玩家收到 ephemeral 錯誤，Railway log 有 traceback。
- [ ] 快速連點任一交易按鈕：同一張面板只允許第一筆交易，第二筆顯示「上一筆操作正在處理」。

## 4. 城下町入口

- [ ] 農牧師、漁採師、魔晶礦師、灶台料理、工坊總覽、背包與出售、休息片刻均可進入。
- [ ] 每個子頁的「返回生活職業」、「返回城下町」、「返回我的面板」都能回到正確頁面。
- [ ] 所有頁面圖片存在時能載入；暫時移走一張測試圖片時，互動仍成功且只略過圖片。

## 5. 背包與出售

- [ ] 全空背包與單一空分類可開啟，不產生空 Select。
- [ ] 只有一頁時上一頁／下一頁狀態正確。
- [ ] 放入超過五種同分類物品，測試多頁與跨分類翻頁。
- [ ] 切換分類與選擇物品後，embed、數量、縮圖一致。
- [ ] 詳細說明在圖片存在／不存在時都能回覆。
- [ ] 精神力滿但體力不足時食用料理：只增加體力並扣一份料理。
- [ ] 體力滿但精神力不足時食用料理：只增加精神力並扣一份料理。
- [ ] 兩者都滿時料理不扣除。
- [ ] 體力藥水恢復體力，不占用料理每日回體額度。
- [ ] 出售目前分類、出售全部可售物資後，金額與數量正確；工具升級保留材料未被出售。

## 6. 工坊、灶台與交易

- [ ] 三種工具從 Lv.0 到 Lv.5 的費用、材料與精神力顯示正確；Lv.5 不可再升。
- [ ] 麻瓜幣、材料或精神力不足時，三者都不扣。
- [ ] 在測試資料庫強制交易失敗，確認麻瓜幣、精神力、材料與工具等級全部回滾。
- [ ] 工坊總覽與三個工坊都能進入，`TOOL_UPGRADE_SPIRIT_COSTS` 的五級索引不越界。
- [ ] 灶台每個配方都能切換，Select 選項不超過 25。
- [ ] 材料不足時只顯示安全提示，材料與背包不變。
- [ ] 製作成功後材料扣除、料理增加一份。

## 7. 每日重置

- [ ] 在 staging 使用可控制時間的資料庫副本測試台灣時間 23:59 → 00:00。
- [ ] 跨日後體力、每日休息次數與料理每日回體額度重置；精神力保留跨日前數值。
- [ ] 分別由 `/我的`、`/學生資料`、`/城下町` 觸發跨日檢查。
- [ ] 同一天重複開面板不會重複贈送體力或重置已使用次數。

## 8. 上線與回復

- [ ] 小範圍 staging／測試伺服器完成以上項目後才部署正式環境。
- [ ] 正式啟動後立刻查看 Railway log，確認沒有 schema、缺檔、import 或 Discord callback 錯誤。
- [ ] 抽查至少一位既有玩家的學籍、地點、神諭、麻瓜幣、體力、精神力、工具與背包。
- [ ] 若筆數或玩家數值異常，立刻停止 Bot，保留異常資料庫副本供比對，再以部署前備份回復；不要在原檔反覆執行 migration。
